// Feature flags for toggling features on/off
// Set to true to enable, false to disable

export const featureFlags = {
  // Spotify integration - disabled until API keys are configured
  // Enable this once Spotify Developer Platform allows new app creation
  spotify: false,
} as const;
